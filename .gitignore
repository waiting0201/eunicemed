**/bin/
**/obj/
**/temp/

.vs/
.vscode/

.DS_Store
Thumbs.db

# Azurite Storage Emulator
AzuriteConfig
AzuriteConfig/
__azurite_db_*__.json
__blobstorage__/
__queuestorage__/
.azurite/
azurite-debug.log

# Node
**/node_modules/
**/dist/
.next/

# TypeScript incremental compilation
*.tsbuildinfo

# Azure Functions local settings (contains secrets — never commit)
Api/local.settings.json
**/local.settings.json

# 本機產出檔（規劃 PDF 等 — 不進版控）
output/

# Cloudflare Pages CLI 狀態（cache/pages.json 內含 account_id — 不進版控）
.wrangler/

# 版型迭代（各約 38MB）。mockup4 是客戶定案的那一版，仍在本機使用。
mockup/
mockup2/
mockup3/
mockup4/

# 品牌方與代理商提供的素材：logo 規範、.ai 原始檔、網站規劃書、
# 以及 Myriad Variable Concept 字型檔（Adobe 商業字型，授權待法務確認，
# 見 docs/08-design.md §4）。
#
# **這些是客戶的機密素材，不進版控** —— 本 repo 是公開的，
# 而圖片與字型一旦進了 git 歷史就拿不掉（fork 與快取會留存）。
# 檔案仍留在本機工作目錄，取得方式見 docs/14-assets.md。
reference/
