### Phase 5 驗收：頁面區段 + JSON Schema registry
### 前提：func start 執行中；已跑過 Phase 3 上傳幾張圖。

@host = http://localhost:7071/api

### 0. 登入
# @name login
POST {{host}}/auth/login
Content-Type: application/json

{ "email": "admin@eunicemed.local", "password": "REPLACE_WITH_LOCAL_SEED_PASSWORD" }

@at = {{login.response.body.data.accessToken}}

###############################################################################
# 1. Schema registry
#
# registry 在**程式碼**裡不在資料庫（docs/09 §9.1）：版面變更走 PR，
# 不是編輯者按一按就能改。檔名即 {pageKey}.{sectionKey}.json，EmbeddedResource。
###############################################################################

### 1a. 該頁全部區段的 schema（後台據此生成表單）
GET {{host}}/admin/page-schema/about
Authorization: Bearer {{at}}

> 驗收：`milestones.properties.background` 應含由 `x-mediaPreset` 展開的
> `x-recommendedWidth: 2560`、`x-aspect: "2.84:1"`、`x-hint`。
> **schema 檔本身不得出現任何尺寸數字**（docs/09 §9.1 的驗收條件）：
>   grep -l "1200\|2560" Api/PageSchemas/*.json   # 應無輸出

### 1b. 18 頁清單
GET {{host}}/admin/pages
Authorization: Bearer {{at}}

###############################################################################
# 2. Schema 驗證 → JSON Pointer
#
# 錯誤每一項以 JSON Pointer 開頭（docs/04 §3.2）。這是選 JsonSchema.Net 的主因：
# 它的 EvaluationResults.InstanceLocation 本身就是 JSON Pointer，不需要轉換層。
###############################################################################

### 2a. maxLength 超限 → 400，錯誤指到 /items/1
PUT {{host}}/admin/pages/about/sections/milestones
Authorization: Bearer {{at}}
Content-Type: application/json

{ "locale": "en",
  "data": { "items": [
    { "year": "2008", "event": "Founded" },
    { "year": "THIS-YEAR-STRING-IS-DELIBERATELY-FAR-TOO-LONG-FOR-THE-FIELD", "event": "x" }
  ]}}

### 2b. minItems 未達（items 要 3–12）→ 400
PUT {{host}}/admin/pages/about/sections/milestones
Authorization: Bearer {{at}}
Content-Type: application/json

{ "locale": "en", "data": { "items": [{ "year": "2008", "event": "Founded" }] } }

### 2c. additionalProperties: false → 多餘欄位被擋
###
### 用 `band` 當那個「合法欄位」—— about.hero 收斂後只剩它（docs/15）。
### 若這裡送已經被移除的 `title`，測試會因為錯的理由通過。
PUT {{host}}/admin/pages/about/sections/hero
Authorization: Bearer {{at}}
Content-Type: application/json

{ "locale": "en", "data": { "band": "{mediaId}", "strayField": "should be rejected" } }

### 2d. 讀取邊界會剪掉 schema 已不存在的欄位（docs/15 §4.1）
###
### 把 2e 拿到的區段內容**原樣送回**應該是 200 —— 修好之前，一支收斂過的 schema
### 會讓編輯者每次儲存都撞上 2c 的 400，而錯誤指向他畫面上根本看不到的欄位。
GET {{host}}/admin/pages/home
Authorization: Bearer {{at}}

> 驗收：`featuredProducts` 只有 `promo`、`bodyPartBand` 只有 `background`，
> 不再出現 `title` / `allLink` / `lead` / `cta` / `tiles` 這些已移除的欄位。

###############################################################################
# 3. 正常儲存（把 {mediaId} 換成 GET /admin/media 取得的 id）
###############################################################################

### 3a. milestones（en，含 media 與 x-localeInvariant 的 year）
PUT {{host}}/admin/pages/about/sections/milestones
Authorization: Bearer {{at}}
Content-Type: application/json

{ "locale": "en",
  "syncInvariantFields": true,
  "data": {
    "background": "{mediaId}",
    "items": [
      { "year": "2008", "event": "Comfort Plus founded in New Taipei City." },
      { "year": "2015", "event": "ISO 13485 certification achieved." },
      { "year": "2023", "event": "EuniceMed brand launched internationally." }
    ]}}

### 3b. certificates（en，含 ref:Certification）
PUT {{host}}/admin/pages/about/sections/certificates
Authorization: Bearer {{at}}
Content-Type: application/json

### 收斂後只剩引用清單 —— 標題／導言／CTA 已改為前端常數（docs/15 §3）
{ "locale": "en",
  "data": {
    "items": [{ "certification": "iso-13485" }, { "certification": "ce" }, { "certification": "mit" }]
  }}

###############################################################################
# 4. 跨語系同步（docs/09 §9.3）
#
# 存 en 並勾 syncInvariantFields 之後：
#   - 尚不存在的語系列會**被補建**（否則第一次編輯永遠同步不到，
#     那正是這個功能要消除的成本）
#   - media / link URL / 年份等標了 x-localeInvariant 的欄位會被帶過去
#   - **文字欄位不同步**
###############################################################################

### 4a. 檢查兩個語系的內容
GET {{host}}/admin/pages/about
Authorization: Bearer {{at}}

> 驗收：以 **milestones** 看 —— zh-TW 應有 `background` 與各項的 `year`（都是 invariant），
> 但 `event` 為空（文字不同步）。
>
> ⚠️ 不要拿 certificates 當這個示範：它收斂後**全部欄位都是 invariant**，
> 兩種行為只看得到一種。

###############################################################################
# 5. 公開端點的回應契約（docs/04 §4）
###############################################################################

### 5a. en
GET {{host}}/pages/about?locale=en

### 5b. zh-TW
GET {{host}}/pages/about?locale=zh-TW

> 驗收：
> 1. `sections` 是**物件**（key = sectionKey）不是陣列 —— 順序由前端模板決定
> 2. media 欄位已解析為 `{url, alt, variants[]}`；`ref:Entity` 仍是識別字串
> 3. `refs.certifications` 已帶出 mark / subLabel / description / logo
> 4. **只有 invariant、尚未翻譯的區段不會出現** —— 用 schema 的 `required`
>    當渲染閘門，不需要額外狀態也不會與 schema 脫節
> 5. 缺整個翻譯列的區段同樣不出現（語言純度）

### 5c. 停用區段：**仍然回傳**但帶 _enabled: false（供後台預覽，模板自行略過）
PATCH {{host}}/admin/pages/about/sections/milestones/enabled
Authorization: Bearer {{at}}
Content-Type: application/json

{ "isEnabled": false }

###############################################################################
# 6. 版面鎖定：不提供 POST / DELETE sections
#
# 區段集合由 schema registry 與 seed 同步器決定（docs/09 §0.1）。
# 兩者都應 404 —— 路由根本不存在。
###############################################################################

### 6a. POST → 404
POST {{host}}/admin/pages/about/sections/newone
Authorization: Bearer {{at}}
Content-Type: application/json

{}

### 6b. DELETE → 404
DELETE {{host}}/admin/pages/about/sections/hero
Authorization: Bearer {{at}}

###############################################################################
# 7. 同步器
#
# 每次 Function App 啟動時自動跑一次。移除的區段**標記停用而非硬刪** ——
# 版面改回來時內容還在。
###############################################################################

### 7a. 手動觸發（連跑三次應都是「未變更 N」）
POST {{host}}/admin/maintenance/sync-page-sections
Authorization: Bearer {{at}}
X-Maintenance-Key: dev

### 7b. 缺 X-Maintenance-Key → 403
POST {{host}}/admin/maintenance/sync-page-sections
Authorization: Bearer {{at}}

### 7c. 非 Admin → 403（即使帶了 key）

###############################################################################
# 8. MediaUsage 自動重建
#
# 這是**唯一**能追到藏在 DataJson 裡的 mediaId 的途徑（沒有 FK 可用）。
# 判準是 schema 上的 x-fieldType: "media"，**絕不用 regex 掃 GUID 形狀的字串** ——
# 文章 HTML 或任何欄位都可能剛好含 GUID，那會造出查不出原因的幽靈引用。
###############################################################################

### 8a. 存完區段後，該媒體應被記為引用
GET {{host}}/admin/media/{mediaId}/usages
Authorization: Bearer {{at}}

> 應回 `[{ entity: "PageSection", locale: "en", fieldPath: "background" }]`

### 8b. 該媒體現在刪不掉 → 409
DELETE {{host}}/admin/media/{mediaId}
Authorization: Bearer {{at}}

###############################################################################
# 9. Richtext 淨化（伺服器端，白名單制）
#
# 後台的 TipTap **不是安全邊界** —— 任何人都能直接打這個端點。
# 本站沒有 WAF（docs/07 §7.4），應用層是唯一防線。
#
# 三組白名單（docs/09 §9.2）：
#   Section  p strong em ul ol li a
#   Article  + h2 h3 blockquote figure figcaption img
#   Legal    Section + h2 h3
#
# 淨化在 **schema 驗證之前** 執行：淨化會改變長度，先驗後淨會讓
# 實際儲存的內容沒被驗過。
###############################################################################

### 9a. 逐一送出這些 body，再用 GET /admin/pages/about 檢查儲存結果
PUT {{host}}/admin/pages/about/sections/story
Authorization: Bearer {{at}}
Content-Type: application/json

{ "locale": "en", "data": { "title": "Our story", "body": "<script>alert(1)</script>safe" } }

# 實測結果：
#   <script>alert(1)</script>安全內容        → 安全內容            （連內容一起移除）
#   <style>body{display:none}</style>正常文字 → 正常文字
#   <p onclick="alert(1)">點我</p>            → <p>點我</p>
#   <img src=x onerror=alert(1)>後續文字      → 後續文字            （Section 不允許 img）
#   <a href="javascript:alert(1)">連結</a>    → <a>連結</a>
#   <a href="https://example.com">外部</a>    → 自動加 rel="noopener noreferrer" target="_blank"
#   <a href="/en/about">站內</a>              → 不加 target
#   <h2>標題</h2><p>內文</p>                  → 標題<p>內文</p>     （標籤移除但文字保留）
#
# 注意 script/style 是**連內容一起丟**，其餘標籤是**保留文字**。
# 前者的內容是程式碼不是文字，留下來只會讓攻擊字串顯示在頁面上。

###############################################################################
# 10. SVG 清洗（只有 logo-mark preset 接受 SVG）
#
# 本專案**沒有病毒掃描**（docs/03 §6），格式白名單與清洗器是唯一防線。
# SVG 是 XML，可內嵌 script、外部參照與 CSS import。
###############################################################################

### 10a. 上傳含 script / onclick / 外部 href / foreignObject / @import 的 SVG
#   curl -X POST {{host}}/admin/media -H "Authorization: Bearer $AT" \
#     -F "file=@evil.svg" -F "presetKey=logo-mark"
#
# 應回 201 並帶 svg_sanitized 警告；下載存進 Blob 的檔案檢查：
#   ✓ <script> 已移除          ✓ 事件處理器已移除
#   ✓ 外部參照已移除            ✓ @import 已移除
#   ✓ foreignObject 已移除      ✓ 合法圖形與文字保留

### 10b. SVG 丟到不接受 SVG 的 preset → 415
#   -F "presetKey=square"


###############################################################################
# 媒體 preset 比對（2026-08-18 補）
#
# schema 的 `x-mediaPreset` 宣告該版位要哪個尺寸。JSON Schema 表達不了這條
# （要查 DB 的 `Media.PresetKey`），所以在 Handler 內比對。
#
# ⚠️ 這是**後加的規則**：在它之前寫入的資料不會被回溯檢查，
#    只有下次存檔時才會被擋。清資料時要注意。
###############################################################################

### A. square 的圖塞進 wide-16x9 版位 → 應 400
#   errors 應為：/imageWide: 此版位需要 'wide-16x9' 尺寸的圖，目前選的是 'square'。
PUT {{host}}/admin/pages/about/sections/manufacturing
Authorization: Bearer {{at}}
Content-Type: application/json

{
  "locale": "en",
  "data": {
    "imageWide": "REPLACE_WITH_A_SQUARE_MEDIA_ID",
    "points": [{ "title": "a", "body": "b" }, { "title": "c", "body": "d" }, { "title": "e", "body": "f" }]
  },
  "syncInvariantFields": true
}

### B. 同一個欄位放 square preset 的 imageSquare → 應 200
PUT {{host}}/admin/pages/about/sections/manufacturing
Authorization: Bearer {{at}}
Content-Type: application/json

{
  "locale": "en",
  "data": {
    "imageSquare": "REPLACE_WITH_A_SQUARE_MEDIA_ID",
    "points": [
      { "title": "Precision fit", "body": "Premium Lycra for lasting compression efficacy." },
      { "title": "Safe materials", "body": "Oeko-Tex Standard 100 yarns." },
      { "title": "Comfort-first", "body": "Soft-touch textures for extended wear." }
    ]
  },
  "syncInvariantFields": true
}

### C. 各 About 區段的版位需求（供對照）
#   about.hero.band            → page-band
#   about.story.portrait       → portrait-4x5
#   about.milestones.background→ section-bg
#   about.manufacturing.imageWide   → wide-16x9
#   about.manufacturing.imageSquare → square
GET {{host}}/admin/page-schema/about
Authorization: Bearer {{at}}
